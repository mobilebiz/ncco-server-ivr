{
  "name": "ncco-server-ivr",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio serverless:deploy --override-existing-project"
  },
  "dependencies": {
    "@twilio/runtime-handler": "1.3.0",
    "@vonage/server-sdk": "^3.3.0",
    "twilio": "^3.56"
  },
  "devDependencies": {
    "twilio-run": "^3.5.2"
  },
  "engines": {
    "node": "16"
  }
}
